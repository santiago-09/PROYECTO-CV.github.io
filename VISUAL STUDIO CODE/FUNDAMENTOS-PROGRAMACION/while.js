

var x=10; //inicio la variable

while(x>0){     //la condicion mientras x sea mayor que 0
    console.log("Mi variable vale: " + x); //sentencia
    x--;    //hay que colocar si o si el decremento para la condicion
            //sino puede que la condicion se ejecute hasta el infinito
            //crashea la PC
}